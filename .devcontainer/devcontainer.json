{
	"name": "Plutus development",
	"image": "uwyoiohk/plutus-development:latest",
	"settings": {
		"terminal.integrated.shell.linux": "/usr/bin/zsh",
        "[haskell]": {
            "editor.tabSize": 2,
            "editor.defaultFormatter": "sjurmillidahl.ormolu-vscode"
        },
	},
	"extensions": [
		"haskell.haskell",
		"sjurmillidahl.ormolu-vscode"
	],
    // Mainly helps with maintaining cabal build cache
    "workspaceMount": "source=${localWorkspaceFolder},target=${containerWorkspaceFolder},type=bind",
    "mounts": [
        // Use cabal cache volume
        "source=${localWorkspaceFolder}/.cabal,target=/root/.cabal,type=bind"
    ],
    "postCreateCommand": ["cabal", "update"]
}